import { OtraDirectivaDirective } from './otra-directiva.directive';

describe('OtraDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new OtraDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
